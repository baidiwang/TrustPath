{
  "name": "trustpath",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check assets/app.js && node --check api/extract.js && node --check api/rules.js",
    "build": "mkdir -p dist/assets && cp index.html dist/index.html && cp assets/app.js dist/assets/app.js && cp assets/styles.css dist/assets/styles.css"
  }
}
